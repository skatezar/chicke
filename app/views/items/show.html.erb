<!-- app/views/items/show.html.erb -->

<%= link_to "< BACK", root_path,style: "color:black; text-decoration: none;", class: "text-center mt-3 btn generic-btn" %>
<div class="container">
  <h1 class="text-center mt-3"><%= @item.title %></h1>

  <div class="row mt-4">
    <div class="col-md-6 offset-md-3">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title">Item Details</h5>
          <p class="card-text"><strong>Description:</strong> <%= @item.description %></p>
          <p class="card-text"><strong>Price:</strong> <%= number_to_currency(@item.price) %></p>
        </div>
      </div>
    </div>
  </div>
</div>
